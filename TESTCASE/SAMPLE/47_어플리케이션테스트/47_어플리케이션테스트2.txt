;-----------------------------------------------------------------------------------------------------------------------------------------
;# 설명 : 웹브라저 외에 일반 윈도우 응용프로그램을 대상으로 테스트 
;# GUITAR.INI에 정보 추가 없이 바로 exe 파일로 실행하고 프로세스를 선택 가능
;-----------------------------------------------------------------------------------------------------------------------------------------

;그림판 실행
"$실행=ShellExecute('mspaint.exe')" 계산한다.

; 프로세스 선택
mspaint 프로세스선택한다. ,

; 화면 그리기
"250,250,300,300" 플리킹 한다.
현재 화면을 캡쳐한다.

; 그림판 종료
"$실행=Processclose('mspaint.exe')" 계산한다.
